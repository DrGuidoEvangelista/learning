# the most simple graphs
library(datasets)
#we will use the dataset from mtcars 
?mtcars #for some info about the dataset

barplot(mtcars$cyl) #it works but the graph is shit
#we need to reform the data 
cylinders <- table(mtcars$cyl)  #to create a table
barplot(cylinders)   # Bar chart
plot(cylinders)  # Default X-Y plot (lines)
